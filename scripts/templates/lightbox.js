// openLightbox doit être définie avant l'utilisation dans mediaFactory.js

function openLightbox(id) {
    // Logique pour ouvrir la lightbox
    console.log(`Open lightbox for media with id: ${id}`);
    // Votre logique de lightbox ici...
}